package growthcraft.deco.init.client;

public class GrowthcraftDecoBlockRenderers {

    public static void registerBlockRenders() {
        // Block rendering is now set in the block model json.
    }

    private GrowthcraftDecoBlockRenderers() {
        /* Prevent automatic creation of public constructor. */
    }
}
